apiVersion: v1
kind: Service
metadata:
  name: interceptor-metrics
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: interceptor
    app.kubernetes.io/name: http-add-on
  ports:
    - name: metrics
      protocol: TCP
      port: 2223
      targetPort: metrics
