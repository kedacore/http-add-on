apiVersion: apps/v1
kind: Deployment
metadata:
  name: interceptor
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: interceptor
      app.kubernetes.io/name: http-add-on
  template:
    metadata:
      labels:
        app.kubernetes.io/component: interceptor
        app.kubernetes.io/name: http-add-on
        app.kubernetes.io/part-of: keda
    spec:
      containers:
        - name: interceptor
          image: ko://github.com/kedacore/http-add-on/interceptor
          args:
            - --zap-log-level=info
            - --zap-encoder=console
            - --zap-time-encoding=rfc3339
          env:
            - name: KEDA_HTTP_PROXY_PORT
              value: "8080"
            - name: KEDA_HTTP_ADMIN_PORT
              value: "9090"
          ports:
            - name: admin
              containerPort: 9090
            - name: proxy
              containerPort: 8080
            - name: proxy-tls
              containerPort: 8443
            - name: metrics
              containerPort: 2223
          livenessProbe:
            httpGet:
              path: /livez
              port: admin
          readinessProbe:
            httpGet:
              path: /readyz
              port: admin
          # TODO(pedrotorres): set better default values avoiding overcommitment
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
            limits:
              cpu: 1000m
              memory: 1000Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: interceptor
      terminationGracePeriodSeconds: 45
